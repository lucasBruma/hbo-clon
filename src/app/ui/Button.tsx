interface Props {
    variant?: 'primary' | 'secondary' | 'tertiary'

}

const Button = ({variant}: Props):JSX.Element =>{
    return (
        <button>

        </button>
    )
}

export default Button;